---
description: 基于用户提问，在wiki库中进行多文档关联检索与合成，输出带有来源引用的解答。对于高价值的交叉引用或总结，主动将其固化为新的内容地图(maps)或概念(concepts)页面。
allowed-tools: PowerShell(obsidian*)
---

## 核心原则

1. **路由优先**：在大量读取文件内容前，优先通过 `obsidian base:query path="moc/INDEX.base"` 或读取 `moc/TAGS.md` 进行全景路由，避免盲目读取大量无关文件。
2. **严谨溯源**：生成的回答必须做到“字字有出处”。任何观点或数据必须通过 Obsidian Flavored Markdown 规范（wikilink `[[来源页面]]` 或 callout 块）进行引用。
3. **知识沉淀**：Query 不应仅仅是阅后即焚的对话。如果本次解答综合了 3 个以上的页面，产生了有价值的新对比、新视角或时间线，应当主动询问并帮助用户将其持久化归档。

## 约束

### 1. 检索与阅读限制

- 单次 `obsidian read` 读取的具体文件数量尽量控制在 5 个以内，避免上下文超载。
- 遵循“顺藤摸瓜”原则：如果文件 A 中高频双链了文件 B，优先将文件 B 加入下一轮读取队列。

### 2. 输出解答格式

最终向用户输出的 Markdown 解答需包含以下结构：

- **核心结论**：一句话或简短列表总结。
- **综合分析**：分点展开，穿插 `[[wikilink]]`。
- **信息盲区**：(可选) 明确指出当前库中缺乏哪些关键信息以完美回答该问题。

### 3. 询问是否归档

不归档则直接结束

### 4. 操作日志格式 (仅在生成了新页面时记录)

```markdown
## [YYYY-MM-DD] summary | {生成的高价值内容}

detail
```
